<!DOCTYPE html>
<html>

<head>
	<meta charset="utf-8">
	<meta name="viewport" content="width=device-width, initial-scale=1">
	<title>Update Password</title>
	<link rel="stylesheet" href="css/updatePassword.css">
</head>

<body>
	<center>
		<div class="box">
			<form action="/changePassword" method="post" enctype="multipart/form-data">
				<img src="<%= img.url %>">
				<input type="file" name="" id="file" accepts="image/*">
				<label for="file"> EDIT PIC</label>

				<input type="password" name="currentPassword" value="<%= cp %>" placeholder="Current_Password">
				<input type="password" name="newPassword" value="<%= np %>" placeholder="New_Password">
				<input type="password" name="confirmPassword" value="<%= CP %>" placeholder="Confirm_Password">
				<button style="float: left;margin: 10px 0 0 18.2%">CANCEL</button>
				<button style="float: right;margin: 10px 18.2% 0 0">DONE</button>
			</form>
		</div>

	</center>
</body>

</html>